# AI Coding Assistant Rules for Antigravity & Claude

이 프로젝트에서는 다음의 핵심 원칙들을 **반드시(MUST)** 준수하십시오.

## 1. ✅ 핵심 원칙 (Core Principles)
1. **적극적 검증 수행 (Active Verification)**:
   - 수정 사항이 적용된 후, `browser_subagent` 또는 터미널 명령어를 사용하여 **반드시 실시간 검증을 수행**하십시오.
   - 워크플로우 실행 상태, 릴리즈 생성 여부, Docker 이미지 메타데이터 등을 AI가 직접 확인하고 보고해야 합니다.
2. **정확성과 신뢰성 우선 (Accuracy & Reliability)**:
   - 사용자가 확인하기 전에 AI가 먼저 오류를 발견하고 수정하는 것을 목표로 합니다.
   - 검증 결과(스크린샷, 로그 등)를 근거로 제시하십시오.
3. **배포 프로세스 준수**:
   - 아래 명시된 배포 4단계를 수행할 때, 각 단계의 결과를 직접 확인하며 진행하십시오.

## 2. 📦 배포 및 버전 관리 (Release & Versioning)
**새로운 버전(Tag)을 생성하거나 배포할 때, 다음 4단계 절차를 따르십시오.**

### Step 1: 코드 버전 동기화 (Update Version File)
* **Rule**: 새로운 버전을 만들기 전, 반드시 `main.go` 파일을 확인하십시오.
* **Action**: `main.go` 내의 버전 명시 부분(예: `var version = "vX.X.X"`)을 찾아 새로운 버전으로 수정하는 코드를 가장 먼저 제안하십시오.

### Step 2: 태그 및 패키지 설명 (Annotated Tags)
* **Rule**: Git Tag나 GitHub Package 생성 시, 빈 메시지를 사용하지 마십시오.
* **Action**:
    * Tag 생성 시: `git tag -a v1.0.0 -m "Release description..."`과 같이 **주석(Annotation)과 설명**을 반드시 포함하십시오.
    * Package: 패키지 메타데이터(OCI Labels)에 `org.opencontainers.image.description`을 포함하고, **패키지 공개 범위(Visibility)가 Public**인지 반드시 확인하십시오.

### Step 3: 릴리즈 노트 작성 (GitHub Release)
* **Rule**: 상세한 Changelog가 포함된 GitHub Release를 생성하고 직접 확인하십시오.
* **Action**: Tag 생성 후에는 Workflow가 정상적으로 Release를 생성했는지 `browser_subagent`로 확인하십시오.

### Step 4: 결과 검증 및 보고 (Post-Deployment Verification)
* **Rule**: 배포 완료 후 사용자가 요청하기 전에 AI가 먼저 정상 동작을 검증하십시오.
* **Action**:
    * `browser_subagent`를 통해 GitHub Releases, Actions, GHCR 메타데이터를 확인하십시오.
    * 터미널 명령어로 빌드된 이미지의 라벨(`docker inspect`) 등을 직접 검증하여 보고하십시오.
    * **로컬 환경 검증**: 배포된 이미지를 로컬로 풀(`docker pull`) 받은 후, 컨테이너를 실행하여 기본적인 헬스체크 및 버전 응답을 직접 확인하십시오.

---
## 3. (Optional) 기술 스택 컨텍스트
* **Language**: Go (Golang)
* **Environment**: GitHub Actions, Container Registry